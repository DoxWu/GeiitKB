# ============================================
# GeiIt企业知识库 - Python 依赖（生产版）
# ============================================
# 安装方法：pip install -r requirements.txt

# ============================================
# Web 框架
# ============================================
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
sse-starlette==2.1.3

# ============================================
# 数据库（PostgreSQL + pgvector）
# ============================================
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
pgvector==0.3.4
alembic==1.13.3

# ============================================
# Redis（缓存 + 限流 + 任务队列）
# ============================================
redis==5.0.8
redis[hiredis]==5.0.8

# ============================================
# Celery（异步任务队列）
# ============================================
celery==5.6.3
flower==2.0.1

# ============================================
# 数据验证和配置
# ============================================
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# ============================================
# 认证授权
# ============================================
python-jose[cryptography]==3.4.0  # H-14: 3.4.0 修复 CVE-2024-33664（JWE 压缩 DoS）和 CVE-2024-33663（公钥签名 JWT）
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # E4-02: 固定 bcrypt 4.0.1，避免 4.1+ 与 passlib 1.7.4 的 __about__ 兼容性问题

# ============================================
# 文档处理 - 基础
# ============================================
python-docx==1.1.2
beautifulsoup4==4.12.3
requests==2.32.3
markdown==3.7

# ============================================
# 文档处理 - 版面分析与表格
# ============================================
PyMuPDF==1.24.10          # PDF 版面分析、图片提取
pdfplumber==0.11.4        # 表格结构化提取
Pillow==10.4.0            # 图像处理

# ============================================
# OCR（扫描件支持）
# ============================================
pytesseract==0.3.13       # Tesseract OCR 封装
# 注意：还需系统安装 Tesseract OCR：apt-get install tesseract-ocr tesseract-ocr-chi-sim

# ============================================
# LLM 和 RAG 框架
# ============================================
langchain==0.3.4
langchain-openai==0.2.3
langchain-text-splitters==0.3.0
openai==1.52.0
# 注：sentence-transformers 和 langchain-community 已移至 requirements-local-ml.txt
# 本地 Embedding 兜底和 CrossEncoder 重排序为可选功能，未安装时自动降级
# 安装方法：pip install -r requirements-local-ml.txt

# ============================================
# 工具库
# ============================================
python-dotenv==1.0.1
loguru==0.7.2
tenacity==9.0.0           # 重试机制
httpx==0.27.2             # 异步 HTTP 客户端
numpy==1.26.4             # 向量计算
tiktoken==0.7.0           # Token 计数

# ============================================
# 邮件发送（Resend HTTP API + SMTP 备用）
# ============================================
aiosmtplib==3.0.2          # 异步 SMTP 客户端（备用通道，本地开发可用）
resend==2.6.0              # Resend 官方 HTTP API SDK（主通道，不受 Railway SMTP 限制）

# ============================================
# 监控与日志
# ============================================
structlog==24.4.0         # 结构化日志
sentry-sdk==2.14.0        # 错误监控（可选）
prometheus-client==0.20.0 # 指标导出（可选）
PyYAML==6.0.2             # 模型服务配置文件解析（providers.yaml）

# ============================================
# 部署
# ============================================
# 注：gunicorn 已移除（entrypoint.sh 直接使用 uvicorn，未引用 gunicorn）
# uvicorn[standard] 已在 Web 框架部分安装，支持生产级 ASGI 服务
