# ============================================
# GeiIt企业知识库 - 开发/测试依赖
# ============================================
# 安装方法：pip install -r requirements-dev.txt
# 说明：此文件包含 requirements.txt 中的所有依赖，
#       额外添加开发、测试、lint 工具，以及本地 ML 可选依赖
#       （开发环境默认启用本地 Embedding 兜底和 CrossEncoder 重排序）。
# ============================================

-r requirements.txt
-r requirements-local-ml.txt

# ============================================
# 测试框架
# ============================================
pytest==8.3.3
pytest-asyncio==1.4.0
pytest-cov==5.0.0          # E3-02: 覆盖率工具
httpx==0.27.2              # FastAPI TestClient 依赖（已在主依赖中）

# ============================================
# 代码质量
# ============================================
ruff==0.6.9                # Python linter + formatter
mypy==1.11.2               # 静态类型检查（可选）
