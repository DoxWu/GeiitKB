# GeiIt企业知识库 - 项目功能详细清单

> **文档作用**：逐项列出当前知识库项目的所有功能模块，将前端与后端的实现整合在一起，便于检查验证及知识库分块检索。
>
> **更新日期**：2026-07-13
>
> **项目技术栈**：FastAPI + PostgreSQL(pgvector) + Redis + Celery + React + Vite + TailwindCSS

---

## 目录

1. [认证与用户管理](#1-认证与用户管理)
2. [文档管理](#2-文档管理)
3. [文档处理流水线](#3-文档处理流水线)
4. [对话问答](#4-对话问答)
5. [检索系统](#5-检索系统)
6. [权限与安全](#6-权限与安全)
7. [运维与监控](#7-运维与监控)
8. [前端基础设施](#8-前端基础设施)

---

## 1. 认证与用户管理

### 1.1 注册申请

| 项目 | 说明 |
|------|------|
| **功能名称** | 用户注册申请（前后端整合） |
| **功能作用** | 用户通过邮箱申请注册账号，需输入两次邮箱地址（一致性校验防输入错误），提交后由管理员审批 |
| **后端实现** | API 路由：`app/api/routes/registration.py`；邮件服务：`app/services/email_service.py`（Resend SMTP）；数据模型：`app/models/registration.py` |
| **前端实现** | 页面：`src/pages/RegisterApplyPage.tsx`；API 封装：`src/api/auth.ts`；表单校验：两次邮箱一致性校验 |
| **技术栈** | FastAPI + Pydantic + Resend SMTP + React + Zustand |
| **核心逻辑** | 1. 前端两次邮箱输入一致性校验<br>2. 后端检查邮箱是否已注册<br>3. 创建注册申请记录（status=pending）<br>4. 发送通知邮件给管理员<br>5. 管理员审批后发送密码设置链接 |
| **输入输出** | **输入**：`{email, email_confirm, reason}`<br>**输出**：`{message: "申请已提交"}` |
| **关键配置** | `EMAIL_ENABLED`、`SMTP_HOST`、`SMTP_PORT`、`ADMIN_NOTIFY_EMAIL`、`PASSWORD_TOKEN_EXPIRE_HOURS=24` |

### 1.2 密码设置

| 项目 | 说明 |
|------|------|
| **功能名称** | 密码设置（Token 链接） |
| **功能作用** | 管理员审批注册申请后，系统发送含 token 的密码设置链接，用户通过链接设置密码 |
| **后端实现** | 路由：`registration.py` + `email_service.py`；Token 生成：`secrets.token_urlsafe(32)`（256 位熵）；Token 存储：SHA-256 哈希后存数据库 |
| **前端实现** | 页面：`src/pages/SetPasswordPage.tsx`；路由：`/set-password/:token` |
| **技术栈** | FastAPI + secrets + hashlib + React Router |
| **核心逻辑** | 1. Token 生成：256 位熵<br>2. Token 存储：SHA-256 哈希（不存明文）<br>3. 一次性使用：设置密码后立即失效<br>4. 24 小时过期<br>5. 限流：每小时最多 5 次 |
| **输入输出** | **输入**：`{token, password, password_confirm}`<br>**输出**：`{message: "密码设置成功"}` |
| **关键配置** | `PASSWORD_TOKEN_EXPIRE_HOURS=24`、Token 限流 5 次/小时 |

### 1.3 用户登录

| 项目 | 说明 |
|------|------|
| **功能名称** | 用户登录（用户名/邮箱双模式） |
| **功能作用** | 支持用户名和邮箱两种方式登录，返回 JWT Access Token 和 Refresh Token |
| **后端实现** | 路由：`app/api/routes/auth.py`；认证模块：`app/core/security.py`；依赖注入：`app/api/deps.py` get_current_user |
| **前端实现** | 页面：`src/pages/LoginPage.tsx`；状态管理：`src/store/authStore.ts`（Zustand）；API 封装：`src/api/auth.ts`；Token 持久化：localStorage |
| **技术栈** | FastAPI + JWT(HS256) + bcrypt + React + Zustand + localStorage |
| **核心逻辑** | 1. 同时查询 `User.username` 和 `User.email`（前端 email 字段作为 username 发送）<br>2. 密码 bcrypt 校验<br>3. 登录失败计数 + 锁定（5 次失败锁定 15 分钟）<br>4. 签发 JWT（Access 15 分钟，Refresh 7 天） |
| **输入输出** | **输入**：`{username, password}`（username 字段可填邮箱）<br>**输出**：`{access_token, refresh_token, token_type, user: {id, username, email, user_type}}` |
| **关键配置** | `ACCESS_TOKEN_EXPIRE_MINUTES=15`、`REFRESH_TOKEN_EXPIRE_DAYS=7`、`LOGIN_FAILURE_LOCK_THRESHOLD=5`、`LOGIN_FAILURE_LOCK_MINUTES=15`、`UserLogin.username` max_length=100 |

### 1.4 游客登录（临时登录）

| 项目 | 说明 |
|------|------|
| **功能名称** | 游客临时登录 |
| **功能作用** | 提供游客登录入口，无需注册即可体验，但限制为 20 次提问，仅可访问公共文档库，不可上传文档 |
| **后端实现** | 路由：`auth.py` guest-login；提问计数：`chat.py` + Redis；上传限制：`documents.py` 用 `get_current_regular_user` 依赖保护 |
| **前端实现** | 页面：`LoginPage.tsx` 游客按钮；状态：`authStore.ts`（user_type='guest'）；文档页：`DocumentsPage.tsx` 隐藏上传入口显示"游客无权提交"提示 |
| **技术栈** | FastAPI + Redis 计数器 + React 条件渲染 |
| **核心逻辑** | 1. 创建 guest 类型用户（`user_type='guest'`）<br>2. Redis 计数器：`guest:questions:{user_id}`，TTL 24 小时<br>3. 每次提问成功后 +1，达到 20 次返回 429<br>4. 文档上传/URL 导入接口拒绝 guest 用户 |
| **输入输出** | **输入**：无（一键登录）<br>**输出**：`{access_token, user: {user_type: "guest"}}` |
| **关键配置** | Guest 提问上限 20 次、Redis TTL 24 小时、`user_type` 字段（regular/guest） |

### 1.5 Token 刷新与登出

| 项目 | 说明 |
|------|------|
| **功能名称** | Token 刷新与登出 |
| **功能作用** | Access Token 过期后用 Refresh Token 获取新 Access Token；登出时使 Token 失效 |
| **后端实现** | 路由：`auth.py` refresh/logout；登出时将 Token 加入 Redis 黑名单（TTL = Token 剩余有效期） |
| **前端实现** | API 拦截器：`src/api/client.ts`（401 时自动刷新）；登出：`authStore.ts` logout() |
| **技术栈** | FastAPI + Redis 黑名单 + axios 拦截器 |
| **核心逻辑** | 1. Refresh Token 校验有效性<br>2. 签发新的 Access Token<br>3. 登出时将 Token 加入 Redis 黑名单 |
| **输入输出** | **输入**：`{refresh_token}`<br>**输出**：`{access_token, token_type}` |
| **关键配置** | `SECRET_KEY`、`ALGORITHM=HS256`、Redis 黑名单 TTL |

---

## 2. 文档管理

### 2.1 文档上传

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档上传（异步处理） |
| **功能作用** | 用户上传 PDF/Word/Markdown/TXT 文件到个人或公共文档库，触发异步处理流水线 |
| **后端实现** | 路由：`app/api/routes/documents.py` upload_document；异步任务：`app/tasks/document_tasks.py` process_document_task；文件处理：`app/services/document_processor.py` |
| **前端实现** | 页面：`src/pages/DocumentsPage.tsx`；组件：`src/components/documents/UploadZone.tsx`（拖拽上传 + 进度条 + 取消）；API：`src/api/document.ts` uploadDocument() |
| **技术栈** | FastAPI UploadFile + Celery + React 拖拽 + XMLHttpRequest 进度 |
| **核心逻辑** | 1. 文件类型白名单 + MIME 双重校验<br>2. 分块写入防 OOM<br>3. 文件哈希去重（Redis 分布式锁防 TOCTOU）<br>4. 创建 Document 记录（status=pending）<br>5. 派发 Celery 异步任务<br>6. guest 用户被拒 |
| **输入输出** | **输入**：`multipart/form-data: {file, visibility, folder_id?}`<br>**输出**：`{id, title, status: "pending", task_id, ...}` |
| **关键配置** | `ALLOWED_FILE_TYPES=[.pdf, .md, .txt, .markdown, .docx, ...]`、`MAX_FILE_SIZE=50MB`、`RATE_LIMIT_UPLOAD_PER_HOUR=20` |

### 2.2 URL 导入

| 项目 | 说明 |
|------|------|
| **功能名称** | URL 导入文档（SSRF 防护） |
| **功能作用** | 通过 URL 导入网页内容，流式下载并解析为知识库文档 |
| **后端实现** | 路由：`documents.py` import_from_url；URL 校验：`app/core/url_validator.py`；解析器：`app/services/document_pipeline/url_parser.py` |
| **前端实现** | 组件：`src/components/documents/UrlImportModal.tsx`；API：`src/api/document.ts` importFromUrl() |
| **技术栈** | FastAPI + httpx 流式下载 + BeautifulSoup + React Modal |
| **核心逻辑** | 1. SSRF 校验：协议白名单、IP 黑名单、端口黑名单<br>2. 流式下载（累计字节数，超限中止）<br>3. HTML → 纯文本转换<br>4. URL 哈希去重<br>5. 非网页资源（图片/PDF）返回 URL_NOT_HTML 提示 |
| **输入输出** | **输入**：`{url, title?, visibility?}`<br>**输出**：`{id, title, status: "pending"}` |
| **关键配置** | `URL_IMPORT_MAX_SIZE=50MB`、`is_development` 控制是否允许内网地址 |

### 2.3 文档列表与筛选（含排序）★ 排序修复

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档列表查询（分页 + 筛选 + 排序） |
| **功能作用** | 分页查询文档列表，支持按分支、可见性范围、状态、关键词筛选，支持多维度排序 |
| **后端实现** | 路由：`documents.py` list_documents；查询参数：page/page_size/cursor/search/status/scope/folder_id/sort_by/sort_order；排序白名单映射防 SQL 注入 |
| **前端实现** | 页面：`DocumentsPage.tsx`；列表：`DocumentList.tsx`；排序：`SortDropdown.tsx`；搜索：`SearchBar.tsx`；分页：`Pagination.tsx`；状态：`documentStore.ts`（Zustand） |
| **技术栈** | FastAPI Query + SQLAlchemy order_by + PostgreSQL 全文检索 + React + Zustand |
| **核心逻辑** | 1. 权限过滤：普通用户看自己的+公共库，管理员看全部<br>2. scope 范围：accessible/mine/public<br>3. folder_id 过滤：≥1 指定分支，0 未分类，None 不过滤<br>4. **排序**：sort_by（created_at/updated_at/file_name/file_type）+ sort_order（asc/desc），经 Query pattern + 白名单映射双重校验<br>5. 全文检索：to_tsvector + ilike 降级<br>6. 游标分页：大数据量时用 cursor 避免 offset 深翻页 |
| **输入输出** | **输入**：`?page=1&page_size=20&scope=accessible&folder_id=0&sort_by=created_at&sort_order=desc`<br>**输出**：`{items: [...], total, page, page_size, next_cursor}` |
| **关键配置** | 默认 page_size=20、排序默认 created_at desc、`SORT_FIELD_MAP` 白名单 |

### 2.4 文档预览

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档在线预览 |
| **功能作用** | 在线预览文档内容，支持文本、Markdown、PDF、DOCX |
| **后端实现** | 路由：`documents.py` get_document_content；按类型返回：文本(PlainTextResponse)/PDF(FileResponse)/DOCX(python-docx 提取文本) |
| **前端实现** | 组件：`src/components/documents/DocumentPreview.tsx`；Markdown 渲染：`MarkdownRenderer.tsx`（代码高亮 + KaTeX 公式） |
| **技术栈** | FastAPI FileResponse + python-docx + React + KaTeX |
| **核心逻辑** | 1. 权限校验<br>2. 防路径遍历（仅取 basename）<br>3. 文本类：读取返回（>10MB 拒绝预览）<br>4. PDF：文件流内联预览<br>5. DOCX：提取段落文本 |
| **输入输出** | **输入**：`GET /documents/{id}/content`<br>**输出**：PlainTextResponse / FileResponse |
| **关键配置** | `_TEXT_PREVIEW_MAX_SIZE=10MB` |

### 2.5 文档安全重命名

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档安全重命名 |
| **功能作用** | 修改文档标题，需权限校验（仅文档所有者或管理员可操作），输入经安全校验防 XSS |
| **后端实现** | 路由：`documents.py` rename_document；安全校验：`app/core/url_validator.py` validate_document_title() |
| **前端实现** | 组件：`DocumentPreview.tsx` 内联编辑；API：`document.ts` |
| **技术栈** | FastAPI + Pydantic + html.escape + React |
| **核心逻辑** | 1. 权限校验（所有者或管理员）<br>2. 输入校验：HTML 转义防 XSS、路径遍历检测、控制字符过滤<br>3. 更新 Document.title<br>4. 记录审计日志 |
| **输入输出** | **输入**：`{title}`<br>**输出**：`{id, title: "新标题"}` |
| **关键配置** | `validate_document_title()` 安全校验函数 |

### 2.6 文档删除（软删除）

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档删除（软删除） |
| **功能作用** | 删除文档（软删除，保留数据可恢复），同时删除关联的向量数据 |
| **后端实现** | 路由：`documents.py` delete_document；向量删除：`vector_store.py` delete_document_chunks() |
| **前端实现** | 组件：`DocumentItem.tsx` 删除按钮（confirm 确认）；API：`document.ts` deleteDocument() |
| **技术栈** | FastAPI + SQLAlchemy + pgvector + React |
| **核心逻辑** | 1. 权限校验<br>2. 设置 `is_deleted=True`<br>3. **先 commit DB 再删向量**（C-4 修复：DB 一致性优先）<br>4. 删除 pgvector 向量数据<br>5. 记录审计日志 |
| **输入输出** | **输入**：`DELETE /documents/{id}`<br>**输出**：204 No Content |
| **关键配置** | 软删除字段 `is_deleted`、`deleted_at` |

### 2.7 文档重处理

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档重新处理 |
| **功能作用** | 重新执行文档处理流水线（删除旧分块+重新分块+重新嵌入），用于参数调整后或处理失败后重试 |
| **后端实现** | 路由：`documents.py` reprocess_document；任务：`document_tasks.py` reprocess_document_task |
| **前端实现** | 组件：`DocumentItem.tsx` 重新处理按钮（仅 failed 状态显示）；API：`document.ts` reprocessDocument() |
| **技术栈** | FastAPI + Celery + Redis 分布式锁 |
| **核心逻辑** | 1. 权限校验<br>2. 幂等性检查（processing 状态拒绝）<br>3. 分布式锁防并发（TTL=REPROCESS_LOCK_TTL）<br>4. 持锁期间设为 processing（关闭 TOCTOU 窗口）<br>5. 派发 Celery 任务 |
| **输入输出** | **输入**：`POST /documents/{id}/reprocess`<br>**输出**：`{id, status: "processing", task_id}` |
| **关键配置** | `REPROCESS_LOCK_TTL=1800`、`RedisManager.acquire_lock` |

### 2.8 文档库分支管理

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档库分支 CRUD |
| **功能作用** | 文档库分支（Folder）的增删改查，支持文档分类管理。删除分支时文档 folder_id 置 NULL（不删文档） |
| **后端实现** | 路由：`app/api/routes/folders.py`；模型：`app/models/document_folder.py`；Schema：`app/schemas/document_folder.py` |
| **前端实现** | 侧边栏：`Sidebar.tsx`；分支项：`FolderItem.tsx`；创建弹窗：`CreateFolderModal.tsx`；API：`document.ts` getFolders/createFolder/updateFolder/deleteFolder |
| **技术栈** | FastAPI + SQLAlchemy + React + Zustand |
| **核心逻辑** | 1. 分支是用户私有的（user_id 隔离）<br>2. 同一用户下分支名唯一（UniqueConstraint）<br>3. 删除分支时文档 folder_id 置 NULL（ondelete=SET NULL）<br>4. document_count 运行时聚合（不持久化） |
| **输入输出** | **输入**：`{name}`<br>**输出**：`{id, name, document_count, created_at, updated_at}` |
| **关键配置** | `UniqueConstraint("user_id", "name")` |

### 2.9 移动文档到分支

| 项目 | 说明 |
|------|------|
| **功能名称** | 移动文档到其他分支 |
| **功能作用** | 将文档从一个分支移动到另一个分支，或移出分支（归入未分类） |
| **后端实现** | 路由：`documents.py` move_document；更新 Document.folder_id |
| **前端实现** | 弹窗：`MoveToFolderModal.tsx`；API：`document.ts` moveDocument() |
| **技术栈** | FastAPI + SQLAlchemy + React Modal |
| **核心逻辑** | 1. 权限校验（所有者或管理员）<br>2. 验证目标分支存在且属于当前用户<br>3. 更新 folder_id<br>4. 超管操作记录审计日志 |
| **输入输出** | **输入**：`PATCH /documents/{id}/move?folder_id={id}`<br>**输出**：更新后的文档信息 |
| **关键配置** | folder_id=null 表示移出分支 |

### 2.10 未分类文档（默认分支）★ 新增

| 项目 | 说明 |
|------|------|
| **功能名称** | 未分类文档视图（默认分支） |
| **功能作用** | 未指定分支的文档（folder_id IS NULL）归入"未分类"虚拟分支，方便用户分类管理 |
| **后端实现** | 路由：`documents.py` list_documents；folder_id=0 作为哨兵值，过滤 `Document.folder_id IS NULL` |
| **前端实现** | 侧边栏：`Sidebar.tsx` 新增"未分类"入口（Inbox 图标）；常量：`UNCLASSIFIED_FOLDER_ID=0`；标题：`DocumentsPage.tsx` 显示"未分类" |
| **技术栈** | FastAPI Query(ge=0) + SQLAlchemy IS NULL + React |
| **核心逻辑** | 1. folder_id=0 哨兵值表示"未分类"<br>2. 后端 `Document.folder_id.is_(None)` 过滤<br>3. 前端 `folder_id !== undefined` 传参（0 是 falsy，不能用 `if(folder_id)`）<br>4. 侧边栏"未分类"按钮调用 `selectFolder(0)` |
| **输入输出** | **输入**：`?folder_id=0`<br>**输出**：folder_id 为 NULL 的文档列表 |
| **关键配置** | `UNCLASSIFIED_FOLDER_ID=0`、`folder_id: Optional[int] = Query(None, ge=0)` |

### 2.11 文档多选与批量操作 ★ 新增

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档多选 + 批量删除/批量移动 |
| **功能作用** | 用户可多选文档，批量删除或批量移动到分支，减少逐个操作的网络开销 |
| **后端实现** | 路由：`documents.py` batch_delete_documents + batch_move_documents；Schema：`schemas/document.py` BatchDeleteRequest/BatchMoveRequest/BatchOperationResponse |
| **前端实现** | 工具栏：`BatchActionBar.tsx`（全选/批量移动/批量删除/退出）；批量移动弹窗：`BatchMoveModal.tsx`；文档项复选框：`DocumentItem.tsx`；状态：`documentStore.ts`（selectedDocIds/selectionMode） |
| **技术栈** | FastAPI + Pydantic + React + Zustand(Set) |
| **核心逻辑** | 1. 多选模式：点击"多选"按钮进入，文档项显示复选框<br>2. 批量删除：逐个校验权限，无权文档记入 failed 列表（不影响其他）<br>3. 批量移动：提前校验目标分支，逐个更新 folder_id<br>4. 每个文档单独权限校验（权限隔离）<br>5. 限制 1-100 个文档/批 |
| **输入输出** | **输入**：`{document_ids: [1,2,3], folder_id?: 5}`<br>**输出**：`{success_count: 3, failed: [], total: 3}` |
| **关键配置** | `min_length=1, max_length=100`（Pydantic 列表长度校验） |

---

## 3. 文档处理流水线

### 3.1 文档解析

| 项目 | 说明 |
|------|------|
| **功能名称** | 多格式文档解析 |
| **功能作用** | 解析 PDF/DOCX/MD/TXT 文件，提取文本内容和结构化信息 |
| **后端实现** | 模块：`app/services/document_pipeline/`；解析器：PDF（pdfplumber）、DOCX（python-docx）、MD/TXT（直接读取） |
| **前端实现** | 进度展示：`DocumentItem.tsx` 处理进度条；轮询：`documentStore.ts` startDocumentPoll() |
| **技术栈** | pdfplumber + python-docx + Celery 进度回调 |
| **核心逻辑** | 1. 根据文件类型选择解析器<br>2. PDF：逐页提取文本+页码<br>3. DOCX：段落+表格提取<br>4. MD：保留 Markdown 语法<br>5. 输出 PipelineContext |
| **输入输出** | **输入**：文件路径<br>**输出**：`PipelineContext {pages, tables, images, cleaned_text}` |
| **关键配置** | `ENABLE_OCR=True`、`ENABLE_VISION=True`、`ENABLE_TABLE_EXTRACTION=True` |

### 3.2 OCR 图片识别

| 项目 | 说明 |
|------|------|
| **功能名称** | OCR 文字识别 |
| **功能作用** | 识别扫描件 PDF 中的文字，将图片转为可检索文本 |
| **后端实现** | 模块：`app/services/document_pipeline/ocr_processor.py`；引擎：Tesseract / 云服务 |
| **前端实现** | 无独立 UI（后台处理，进度通过轮询显示） |
| **技术栈** | Tesseract OCR + PIL |
| **核心逻辑** | 1. 检测 PDF 是否为扫描件（文本层是否为空）<br>2. 渲染页面为图片<br>3. OCR 识别文字<br>4. 识别结果作为页面文本 |
| **输入输出** | **输入**：PDF 页面图片<br>**输出**：`{text, confidence}` |
| **关键配置** | `ENABLE_OCR=True` |

### 3.3 多模态图片理解

| 项目 | 说明 |
|------|------|
| **功能名称** | 图片内容理解（多模态 LLM） |
| **功能作用** | 用多模态 LLM 理解文档中的图片内容，生成文字描述用于检索 |
| **后端实现** | 模块：`app/services/document_pipeline/`；模型：`VISION_MODEL_NAME` |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | OpenAI Vision API / 兼容多模态 LLM |
| **核心逻辑** | 1. 提取文档中的图片<br>2. 调用多模态 LLM 生成图片描述<br>3. 描述作为独立 chunk 参与检索 |
| **输入输出** | **输入**：图片二进制<br>**输出**：`{description, image_data}` |
| **关键配置** | `ENABLE_VISION=True`、`VISION_MODEL_NAME=gpt-4o-mini` |

### 3.4 表格结构化提取

| 项目 | 说明 |
|------|------|
| **功能名称** | 表格 Markdown 提取 |
| **功能作用** | 提取文档中的表格，转为 Markdown 格式作为独立 chunk |
| **后端实现** | 模块：`app/services/document_pipeline/` |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | pdfplumber 表格提取 + Markdown 格式化 |
| **核心逻辑** | 1. 检测文档中的表格<br>2. 提取表格结构和数据<br>3. 转为 Markdown 表格语法<br>4. 表格作为独立块（不切分） |
| **输入输出** | **输入**：文档表格<br>**输出**：`ExtractedTable {markdown, rows, caption}` |
| **关键配置** | `ENABLE_TABLE_EXTRACTION=True` |

### 3.5 LaTeX 公式保护

| 项目 | 说明 |
|------|------|
| **功能名称** | LaTeX 公式保护 |
| **功能作用** | 分块前用占位符替换 LaTeX 公式，分块后恢复，避免公式被截断 |
| **后端实现** | 模块：`app/services/document_pipeline/latex_protector.py` |
| **前端实现** | 公式渲染：`MarkdownRenderer.tsx`（KaTeX） |
| **技术栈** | 正则匹配 + 占位符替换 + KaTeX 渲染 |
| **核心逻辑** | 1. 正则匹配 `$$...$$` 和 `$...$`<br>2. 替换为占位符 `LATEX_PLACEHOLDER_N`<br>3. 分块器对占位符文本切分<br>4. 分块后恢复占位符为原始公式 |
| **输入输出** | **输入**：含 LaTeX 的文本<br>**输出**：`{protected_text, placeholders}` |
| **关键配置** | `ENABLE_LATEX_PROTECTION=True` |

### 3.6 智能分块

| 项目 | 说明 |
|------|------|
| **功能名称** | 递归字符分块 |
| **功能作用** | 将清洗后的文本切分为合适大小的块（chunk），用于后续向量化 |
| **后端实现** | 模块：`app/services/document_pipeline/chunker.py`；工具：`RecursiveCharacterTextSplitter`（LangChain） |
| **前端实现** | 分块数展示：`DocumentItem.tsx`（`{chunk_count} 个分块`） |
| **技术栈** | LangChain RecursiveCharacterTextSplitter |
| **核心逻辑** | 1. 递归分隔符：`\n\n` → `\n` → `。` → `.` → `！` → `？` → `；` → 空格<br>2. 表格独立成块<br>3. 图片描述独立成块<br>4. 每块携带元数据（页码、位置、类型） |
| **输入输出** | **输入**：`PipelineContext`<br>**输出**：`List[ProcessedChunk]` |
| **关键配置** | `CHUNK_SIZE=800`、`CHUNK_OVERLAP=100` |

### 3.7 向量化嵌入

| 项目 | 说明 |
|------|------|
| **功能名称** | 文本向量化嵌入 |
| **功能作用** | 将文本块转为向量，存入 pgvector 数据库用于相似度检索 |
| **后端实现** | 模块：`app/services/vector_store.py` + `app/services/openai_embedding.py`；嵌入服务：阿里云 text-embedding-v4（主）+ 本地模型（兜底） |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | OpenAI SDK + 阿里云 text-embedding-v4 + pgvector + BAAI/bge-small-zh-v1.1 |
| **核心逻辑** | 1. 调用 Embedding API 将文本转为向量<br>2. 主服务不可用时降级到本地模型<br>3. 向量存入 document_chunks 表（vector(1536)）<br>4. 阿里云需传 `dimensions=1536` 参数 |
| **输入输出** | **输入**：`List[ProcessedChunk]`<br>**输出**：写入 document_chunks 表 |
| **关键配置** | `EMBEDDING_MODEL_NAME`、`EMBEDDING_DIMENSION=1536`、`LOCAL_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.1` |

---

## 4. 对话问答

### 4.1 非流式问答

| 项目 | 说明 |
|------|------|
| **功能名称** | 知识库问答（非流式） |
| **功能作用** | 用户提问，系统检索知识库文档片段，调用 LLM 生成回答，返回引用来源 |
| **后端实现** | 路由：`app/api/routes/chat.py` ask_question；服务：`app/services/rag_chain.py` RAGChainService.ask() |
| **前端实现** | 页面：`src/pages/ChatPage.tsx`；API：`src/api/chat.ts`；引用展示：`SourceCard.tsx`（含置信度标签） |
| **技术栈** | FastAPI + LangChain + DeepSeek LLM + React |
| **核心逻辑** | 1. 意图识别（kb_query → 检索；chitchat/followup/meta → 无检索）<br>2. Query 改写（指代消解+语义扩展）<br>3. 检索文档片段<br>4. 预生成校验<br>5. 构建 Prompt + 调用 LLM<br>6. 矛盾检测<br>7. 保存对话记录 |
| **输入输出** | **输入**：`{conversation_id, question}`<br>**输出**：`{answer, sources, question, intent, metrics}` |
| **关键配置** | `SYSTEM_PROMPT`、`ENABLE_QUERY_REWRITE=True`、`RATE_LIMIT_ASK_PER_MINUTE=20` |

### 4.2 流式问答（SSE）

| 项目 | 说明 |
|------|------|
| **功能名称** | 流式问答（Server-Sent Events） |
| **功能作用** | 流式输出回答，逐字返回提升用户体验 |
| **后端实现** | 路由：`chat.py` ask_question_stream；服务：`rag_chain.py` ask_stream()；FastAPI StreamingResponse |
| **前端实现** | 页面：`ChatPage.tsx`；流式接收：fetch stream + EventSource；渲染：`MessageBubble.tsx` + `MarkdownRenderer.tsx` |
| **技术栈** | FastAPI StreamingResponse + SSE + React fetch stream |
| **核心逻辑** | 1. 同非流式的检索流程<br>2. LLM 流式输出（逐 token 返回）<br>3. SSE 格式：`data: {"content": "..."}\n\n`<br>4. **陷阱**：流式后处理阶段需创建独立 SessionLocal()（请求级 session 可能已失效） |
| **输入输出** | **输入**：`{conversation_id, question}`<br>**输出**：SSE 流 |
| **关键配置** | `LLM_STREAM_FIRST_TOKEN_TIMEOUT=30`（推理模型需调大） |

### 4.3 多轮对话（历史上下文）

| 项目 | 说明 |
|------|------|
| **功能名称** | 多轮对话上下文管理 |
| **功能作用** | 支持多轮对话，保留历史上下文让 LLM 理解追问 |
| **后端实现** | 服务：`rag_chain.py`；模型：`app/models/conversation.py` |
| **前端实现** | 页面：`ChatPage.tsx`；对话列表：侧边栏 conversation 列表 |
| **技术栈** | SQLAlchemy + LangChain memory + React |
| **核心逻辑** | 1. 保留最近 N 轮对话<br>2. 超 Token 上限时截断<br>3. 历史摘要压缩<br>4. 意图切换检测（避免历史污染检索） |
| **输入输出** | **输入**：`{conversation_id, question}`<br>**输出**：含上下文的回答 |
| **关键配置** | `CONVERSATION_HISTORY_LIMIT=5`、`CONVERSATION_HISTORY_MAX_TOKENS=2000` |

### 4.4 历史摘要压缩

| 项目 | 说明 |
|------|------|
| **功能名称** | 历史摘要压缩 |
| **功能作用** | 每隔 N 轮对话生成历史摘要，用摘要替代完整历史，避免 Token 超限 |
| **后端实现** | 服务：`app/services/history_service.py` |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | LangChain + LLM 摘要 |
| **核心逻辑** | 1. 每轮对话后检查是否达到摘要阈值<br>2. 达到阈值时调用 LLM 生成摘要<br>3. 摘要替代旧历史，只保留最近几轮完整对话 |
| **输入输出** | **输入**：对话历史列表<br>**输出**：摘要文本 |
| **关键配置** | `ENABLE_HISTORY_SUMMARY=True`、`SUMMARY_EVERY_N_TURNS=5` |

### 4.5 意图识别

| 项目 | 说明 |
|------|------|
| **功能名称** | 意图识别 + 查询子类型检测 |
| **功能作用** | 识别用户输入意图（知识库提问/闲聊/追问/元问题），决定是否检索文档 + 动态调整检索权重 |
| **后端实现** | 服务：`app/services/intent_classifier.py` IntentClassifier |
| **前端实现** | 无独立 UI（后台处理，结果体现在回答质量） |
| **技术栈** | 规则引擎 + LLM 分类 + 正则匹配 |
| **核心逻辑** | 1. 规则预判（关键词集合+正则，零延迟）<br>2. LLM 分类（规则无法判断时）<br>3. 降级：LLM 不可用时默认 kb_query<br>4. `_detect_query_sub_type()` 检测子类型（EXACT_MATCH/SEMANTIC/HYBRID）→ 动态混合检索权重 |
| **输入输出** | **输入**：`{query, conversation_history}`<br>**输出**：`IntentClassification {intent, confidence, needs_retrieval}` |
| **关键配置** | `ENABLE_INTENT_DETECTION=True`、`QUERY_SUBTYPE_WEIGHTS={exact_match: 0.6, semantic: 0.2, hybrid: 0.3}` |

### 4.6 Query 改写

| 项目 | 说明 |
|------|------|
| **功能名称** | Query 改写（指代消解+语义扩展） |
| **功能作用** | 用 LLM 改写用户问题，提升检索质量 |
| **后端实现** | 服务：`app/services/query_rewrite.py` |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | LangChain + LLM |
| **核心逻辑** | 1. 指代消解："那个怎么样" → 结合历史补全<br>2. 短 query 扩展<br>3. 延续性指令补全<br>4. 超时降级为原始 query |
| **输入输出** | **输入**：`{query, conversation_history}`<br>**输出**：改写后的 query |
| **关键配置** | `ENABLE_QUERY_REWRITE=True`、`QUERY_REWRITE_MIN_LENGTH=5`、`QUERY_REWRITE_TIMEOUT=5` |

### 4.7 矛盾检测

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档片段矛盾检测 |
| **功能作用** | 检测检索到的多个文档片段之间是否存在内容矛盾 |
| **后端实现** | 服务：`app/services/conflict_detector.py` |
| **前端实现** | 无独立 UI（结果体现在回答中） |
| **技术栈** | LLM 判断 |
| **核心逻辑** | 1. 用 LLM 判断多个片段是否冲突<br>2. 检测到矛盾时在上下文标记冲突片段<br>3. 提示 LLM 指出差异 |
| **输入输出** | **输入**：检索结果列表<br>**输出**：`{has_conflict, conflicts}` |
| **关键配置** | `ENABLE_CONFLICT_DETECTION=True` |

### 4.8 引用来源标注

| 项目 | 说明 |
|------|------|
| **功能名称** | 引用来源标注与展示 |
| **功能作用** | 回答中标注引用来源 [文档X]，前端展示来源列表含置信度 |
| **后端实现** | 服务：`rag_chain.py` 构建上下文时编号文档 |
| **前端实现** | 组件：`SourceCard.tsx`（含置信度标签：高/中/低）；渲染：`MessageBubble.tsx` |
| **技术栈** | FastAPI + React + 置信度分级 |
| **核心逻辑** | 1. 检索结果按 document_id 编号<br>2. 上下文中标注编号<br>3. LLM 回答时引用编号<br>4. 返回 sources 列表<br>5. 前端按置信度分级展示 |
| **输入输出** | **输出**：`sources: [{document_id, title, content, score}]` |
| **关键配置** | 置信度分级阈值（高≥0.7 / 中≥0.5 / 低<0.5） |

---

## 5. 检索系统

### 5.1 向量检索（pgvector）

| 项目 | 说明 |
|------|------|
| **功能名称** | 向量相似度检索 |
| **功能作用** | 将用户问题向量化，在 pgvector 中检索相似度最高的文档片段 |
| **后端实现** | 服务：`app/services/vector_store.py` vector_search()；数据库：PostgreSQL + pgvector |
| **前端实现** | 无独立 UI（结果体现在回答来源中） |
| **技术栈** | pgvector + SQLAlchemy + `<=>` 余弦距离操作符 |
| **核心逻辑** | 1. 用户问题 → Embedding 向量<br>2. SQL：`SELECT *, embedding <=> :query_vector AS distance`<br>3. 距离转相似度：`score = 1 - distance` |
| **输入输出** | **输入**：`{query, top_k, document_ids}`<br>**输出**：`[{chunk_id, content, score, document_id}]` |
| **关键配置** | `EMBEDDING_DIMENSION=1536`、`SIMILARITY_THRESHOLD=0.35`、`IVFFLAT_PROBES=50` |

### 5.2 关键词检索（PostgreSQL 全文检索）

| 项目 | 说明 |
|------|------|
| **功能名称** | 关键词检索（中文 bigram + 英文全文检索） |
| **功能作用** | 用 PostgreSQL 全文检索 + 中文 2-gram bigram 进行关键词匹配 |
| **后端实现** | 服务：`vector_store.py` keyword_search()；索引：GIN 索引 |
| **前端实现** | 无独立 UI（结果体现在回答来源中） |
| **技术栈** | PostgreSQL to_tsvector + tsquery + GIN 索引 + ILIKE 模糊匹配 |
| **核心逻辑** | 1. 英文词：`to_tsquery`+`ts_rank` 精确匹配<br>2. 中文：2-gram bigram 滑窗分词 + `ILIKE %term%` OR 模糊匹配<br>3. 按命中数量计分（归一化 0-1）<br>4. ILIKE 通配符转义防 SQL 注入 |
| **输入输出** | **输入**：`{query, top_k, document_ids}`<br>**输出**：`[{chunk_id, content, keyword_score}]` |
| **关键配置** | GIN 索引建在 `to_tsvector('simple', content)` 表达式上 |

### 5.3 混合检索（加权融合）

| 项目 | 说明 |
|------|------|
| **功能名称** | 混合检索（向量+关键词加权融合） |
| **功能作用** | 结合向量检索和关键词检索，加权融合两路结果 |
| **后端实现** | 服务：`vector_store.py` search() |
| **前端实现** | 无独立 UI（结果体现在回答来源中） |
| **技术栈** | pgvector + PostgreSQL 全文检索 + 加权融合算法 |
| **核心逻辑** | 1. 并行执行向量检索和关键词检索<br>2. 按 chunk_id 合并结果<br>3. `final_score = vector_score × vector_weight + keyword_score × keyword_weight`<br>4. 降级：向量检索无结果时回退到仅关键词检索 |
| **输入输出** | **输入**：`{query, top_k, document_ids, keyword_weight}`<br>**输出**：融合排序后的结果 |
| **关键配置** | `ENABLE_HYBRID_SEARCH=True`、`KEYWORD_SEARCH_WEIGHT=0.3`、`candidate_k = top_k × 3` |

### 5.4 动态权重（intent_classifier 复用）★ 任务2新增

| 项目 | 说明 |
|------|------|
| **功能名称** | 动态混合检索权重 |
| **功能作用** | 根据查询子类型动态调整向量/关键词检索权重，替代静态的 0.7/0.3 分配 |
| **后端实现** | 服务：`intent_classifier.py` `_detect_query_sub_type()` + `rag_chain.py` + `vector_store.py` |
| **前端实现** | 无独立 UI（结果体现在检索精度） |
| **技术栈** | 正则匹配 + 权重映射 + 加权融合 |
| **核心逻辑** | 1. EXACT_MATCH：含代码标识符/错误码/引号/版本号 → keyword_weight=0.6<br>2. SEMANTIC：含"如何/为什么/区别/原理" → keyword_weight=0.2<br>3. HYBRID：默认 → keyword_weight=0.3<br>4. 透传 keyword_weight 给 vector_store.search() |
| **输入输出** | **输入**：query 文本<br>**输出**：QuerySubType 枚举值 |
| **关键配置** | `QUERY_SUBTYPE_WEIGHTS={"exact_match": 0.6, "semantic": 0.2, "hybrid": 0.3}` |

### 5.5 Reranking 重排序

| 项目 | 说明 |
|------|------|
| **功能名称** | Cross-encoder 重排序 |
| **功能作用** | 用 cross-encoder 对向量检索的候选结果二次打分，提升 Top-K 精确度 |
| **后端实现** | 服务：`app/services/reranker.py`；模型：sentence-transformers CrossEncoder |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | BAAI/bge-reranker-base + sentence-transformers |
| **核心逻辑** | 1. 向量检索召回 `top_k × multiplier` 个候选<br>2. cross-encoder 对每个 (query, chunk) 对打分<br>3. 按分数排序取 final_top_k |
| **输入输出** | **输入**：`{query, results, top_k}`<br>**输出**：重排序后的 top_k 结果 |
| **关键配置** | `ENABLE_RERANKING=True`、`RERANKER_MODEL_NAME=BAAI/bge-reranker-base`、`RERANKER_CANDIDATE_MULTIPLIER=5` |

### 5.6 权限隔离检索

| 项目 | 说明 |
|------|------|
| **功能名称** | 权限隔离检索 |
| **功能作用** | 检索范围限定为用户有权访问的文档（自己的 + 公共库），防止越权 |
| **后端实现** | 服务：`rag_chain.py` `_compute_search_scope()` + `app/services/permission.py` |
| **前端实现** | 无独立 UI（权限隔离对用户透明） |
| **技术栈** | FastAPI + permission_service + SQL WHERE IN |
| **核心逻辑** | 1. 无 user_id 一律拒绝检索<br>2. 通过 permission_service 计算可访问文档 ID 列表<br>3. 同时有 document_ids 时取交集（最小权限）<br>4. guest 用户仅可访问公共库<br>5. 检索 SQL 加 `WHERE document_id IN (accessible_ids)` |
| **输入输出** | **输入**：`{user_id, document_ids, user_type}`<br>**输出**：`accessible_ids: List[int]` |
| **关键配置** | `user_type` 字段（regular/guest）、permission_service |

### 5.7 FAQ 缓存

| 项目 | 说明 |
|------|------|
| **功能名称** | FAQ 相似问题缓存 |
| **功能作用** | 相同问题不重复调用 LLM，直接返回缓存答案 |
| **后端实现** | 服务：Redis 缓存 |
| **前端实现** | 无独立 UI（对用户透明） |
| **技术栈** | Redis + 向量相似度匹配 |
| **核心逻辑** | 1. 问题向量化后与缓存问题比对<br>2. 相似度 ≥ 阈值时返回缓存答案<br>3. 缓存 TTL 7 天 |
| **输入输出** | **输入**：query 向量<br>**输出**：缓存答案或 None |
| **关键配置** | `ENABLE_FAQ_CACHE=True`、`FAQ_CACHE_SIMILARITY_THRESHOLD=0.95`、`FAQ_CACHE_TTL=604800` |

---

## 6. 权限与安全

### 6.1 JWT 认证

| 项目 | 说明 |
|------|------|
| **功能名称** | JWT 认证（Access + Refresh Token） |
| **功能作用** | 基于 JWT 的用户认证，Access Token + Refresh Token 机制 |
| **后端实现** | 模块：`app/core/security.py`；依赖：`app/api/deps.py` get_current_user |
| **前端实现** | API 拦截器：`src/api/client.ts`（自动携带 Token + 401 刷新）；持久化：localStorage |
| **技术栈** | JWT(HS256) + FastAPI Depends + axios 拦截器 |
| **核心逻辑** | 1. 登录成功后签发 JWT<br>2. 请求头 `Authorization: Bearer <token>`<br>3. 中间件解析并验证 Token<br>4. 从 Token 提取 user_id 查询用户 |
| **输入输出** | **输入**：JWT Token<br>**输出**：User 对象 |
| **关键配置** | `SECRET_KEY`、`ALGORITHM=HS256`、`ACCESS_TOKEN_EXPIRE_MINUTES=15` |

### 6.2 文档访问控制

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档访问权限控制 |
| **功能作用** | 用户只能访问自己上传的文档和公共文档库的文档 |
| **后端实现** | 服务：`app/services/permission.py` permission_service |
| **前端实现** | 条件渲染：`DocumentsPage.tsx`（guest 隐藏上传按钮）；视觉区分：`DocumentItem.tsx`（公共/个人文档库不同颜色） |
| **技术栈** | FastAPI + permission_service + React 条件渲染 |
| **核心逻辑** | 1. 文档可见性：private（仅所有者）/ public（所有用户）<br>2. `get_accessible_document_ids()` 返回可访问文档 ID 列表<br>3. guest 用户仅返回公共文档<br>4. 管理员可访问所有文档 |
| **输入输出** | **输入**：`{db, user_id, user_type}`<br>**输出**：`List[int]` |
| **关键配置** | `Document.visibility` 字段、`User.user_type` |

### 6.3 限流

| 项目 | 说明 |
|------|------|
| **功能名称** | 多维度限流 |
| **功能作用** | 防止滥用和 DDoS |
| **后端实现** | 中间件：`app/middleware/rate_limit_middleware.py`；存储：Redis |
| **前端实现** | 无独立 UI（429 时显示错误提示） |
| **技术栈** | Redis 计数器 + FastAPI middleware |
| **核心逻辑** | 1. 全局限流：100 次/分钟<br>2. 登录限流：5 次/分钟<br>3. 提问限流：20 次/分钟<br>4. 上传限流：20 次/小时<br>5. 可信代理 IP 识别 |
| **输入输出** | **输入**：请求<br>**输出**：正常通过或 429 |
| **关键配置** | `RATE_LIMIT_GLOBAL_PER_MINUTE=100`、`RATE_LIMIT_LOGIN_PER_MINUTE=5`、`RATE_LIMIT_ASK_PER_MINUTE=20`、`RATE_LIMIT_UPLOAD_PER_HOUR=20` |

### 6.4 分布式锁

| 项目 | 说明 |
|------|------|
| **功能名称** | Redis 分布式锁 |
| **功能作用** | 防止并发操作导致的数据不一致 |
| **后端实现** | 模块：`app/core/redis.py` RedisManager |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | Redis + UUID token + Lua CAS 脚本 |
| **核心逻辑** | 1. `acquire_lock(key, ttl)` 获取锁（UUID token + Lua CAS）<br>2. `release_lock(key, token)` 释放锁（token 匹配才释放）<br>3. **注意**：必须用 acquire_lock + release_lock，不能用 set(nx=True) + delete |
| **输入输出** | **输入**：`{key, ttl}`<br>**输出**：`token` 或 `None` |
| **关键配置** | `IDEMPOTENCY_LOCK_TTL=300`、`REPROCESS_LOCK_TTL=1800` |

### 6.5 邮件安全

| 项目 | 说明 |
|------|------|
| **功能名称** | 邮件安全（XSS + CRLF 注入防护） |
| **功能作用** | 防止邮件 XSS 和 header 注入攻击 |
| **后端实现** | 模块：`app/services/email_service.py` |
| **前端实现** | 无独立 UI（后端处理） |
| **技术栈** | Resend SMTP + EmailMessage + html.escape |
| **核心逻辑** | 1. 用户输入用 `html.escape()` 转义防 XSS<br>2. 用 `EmailMessage` 构造 MIME（非字符串拼接）防 CRLF 注入<br>3. SMTP SSL 加密 |
| **输入输出** | **输入**：`{to_email, subject, body}`<br>**输出**：发送状态 |
| **关键配置** | `SMTP_HOST=smtp.resend.com`、`SMTP_PORT=465`、`SMTP_USE_TLS=True`、`SMTP_PASSWORD`（Resend API Key） |

### 6.6 审计日志

| 项目 | 说明 |
|------|------|
| **功能名称** | 操作审计日志 |
| **功能作用** | 记录关键操作（文档删除/重命名/重处理/批量操作等）便于审计追溯 |
| **后端实现** | 服务：`app/services/audit_service.py`；模型：`app/models/audit_log.py` |
| **前端实现** | 无独立 UI（后台记录） |
| **技术栈** | SQLAlchemy + 审计日志表 |
| **核心逻辑** | 1. 记录操作人、操作动作、目标对象、时间戳<br>2. 管理员跨用户操作必记录<br>3. 日志不可篡改（仅追加） |
| **输入输出** | **输入**：`{action, user_id, resource_type, resource_id, detail}`<br>**输出**：审计日志记录 |
| **关键配置** | 超管操作 `_audit_superuser_action()` + `audit_service.log()` |

---

## 7. 运维与监控

### 7.1 LLM 容错

| 项目 | 说明 |
|------|------|
| **功能名称** | LLM 重试+熔断+超时+降级 |
| **功能作用** | LLM 调用的四重容错机制 |
| **后端实现** | 服务：`app/services/llm_resilience.py` LLMResilienceService |
| **前端实现** | 无独立 UI（降级时返回兜底回复） |
| **技术栈** | 指数退避重试 + Redis 熔断器状态共享 |
| **核心逻辑** | 1. 重试：失败后指数退避（1s, 2s, 4s）<br>2. 熔断：连续失败 5 次打开熔断器，60 秒后恢复<br>3. 超时：30 秒<br>4. 降级：LLM 不可用时走兜底回复<br>5. 熔断器状态通过 Redis 在 api/worker 间共享 |
| **输入输出** | **输入**：LLM 调用请求<br>**输出**：LLM 响应或降级回复 |
| **关键配置** | `LLM_MAX_RETRIES=3`、`LLM_TIMEOUT=30`、`CIRCUIT_BREAKER_THRESHOLD=5`、`CIRCUIT_BREAKER_RECOVERY_TIME=60` |

### 7.2 Embedding 容错

| 项目 | 说明 |
|------|------|
| **功能名称** | Embedding 服务容错 |
| **功能作用** | Embedding 服务的容错和降级 |
| **后端实现** | 模块：`vector_store.py` + `app/services/openai_embedding.py` |
| **前端实现** | 无独立 UI（后台处理） |
| **技术栈** | 阿里云 text-embedding-v4 + 本地 BAAI/bge-small-zh-v1.1 |
| **核心逻辑** | 1. 主服务：阿里云 text-embedding-v4（传 dimensions=1536）<br>2. cloud_fallback：备用云服务<br>3. 本地兜底：BAAI/bge-small-zh-v1.1<br>4. **注意**：DeepSeek 不支持 /embeddings，fallback 必须指向阿里云 |
| **输入输出** | **输入**：文本<br>**输出**：向量（1536 维） |
| **关键配置** | `EMBEDDING_MODEL_NAME`、`EMBEDDING_FALLBACK_API_KEY`、`EMBEDDING_FALLBACK_API_BASE` |

### 7.3 健康检查

| 项目 | 说明 |
|------|------|
| **功能名称** | API 健康检查 |
| **功能作用** | API 健康检查端点，用于 Docker 健康检查和监控 |
| **后端实现** | 路由：`app/api/routes/` health 端点 |
| **前端实现** | 无独立 UI |
| **技术栈** | FastAPI + Docker healthcheck |
| **核心逻辑** | 1. 检查数据库连接<br>2. 检查 Redis 连接<br>3. 检查 LLM 服务状态（熔断器状态）<br>4. 返回健康状态 |
| **输入输出** | **输出**：`{status: "healthy", services: {db: "ok", redis: "ok", llm: "ok"}}` |
| **关键配置** | Docker healthcheck `curl -f http://localhost:8000/health` |

### 7.4 Prometheus 指标

| 项目 | 说明 |
|------|------|
| **功能名称** | Prometheus 指标采集 |
| **功能作用** | 暴露 Prometheus 指标端点，采集 API 性能数据 |
| **后端实现** | 路由：`app/api/routes/metrics.py` |
| **前端实现** | 无独立 UI |
| **技术栈** | prometheus-fastapi-instrumentator + Basic Auth |
| **核心逻辑** | 1. 采集 HTTP 请求计数/延迟<br>2. 采集 LLM 调用指标<br>3. 路径标签模板化（避免高基数）<br>4. Basic Auth 保护端点 |
| **输入输出** | **输出**：Prometheus 格式指标文本 |
| **关键配置** | `ENABLE_PROMETHEUS=True`、`PROMETHEUS_METRICS_PATH=/metrics` |

### 7.5 Flower 任务监控

| 项目 | 说明 |
|------|------|
| **功能名称** | Celery 任务监控面板 |
| **功能作用** | 查看 Celery 任务状态/进度/历史 |
| **后端实现** | Docker 服务：flower（profiles: monitoring） |
| **前端实现** | Flower Web 界面（独立服务） |
| **技术栈** | Flower + Celery broker(Redis) |
| **核心逻辑** | 1. 连接 Celery broker<br>2. 展示任务队列/工作者状态<br>3. 任务执行历史和耗时统计 |
| **输入输出** | Web 界面：`http://localhost:5555` |
| **关键配置** | Docker profile `monitoring`（默认不启动） |

### 7.6 数据库迁移（Alembic）

| 项目 | 说明 |
|------|------|
| **功能名称** | 数据库 Schema 版本管理 |
| **功能作用** | 数据库 schema 版本管理，支持升级和回滚 |
| **后端实现** | 工具：Alembic；迁移文件：`backend/alembic/versions/` |
| **前端实现** | 无独立 UI |
| **技术栈** | Alembic + SQLAlchemy |
| **核心逻辑** | 1. 迁移文件按时间戳编号<br>2. `alembic upgrade head` 应用所有待执行迁移<br>3. Railway 配置 `releaseCommand = "alembic upgrade head"`<br>4. `MIGRATE_ON_STARTUP=false`（Railway 用 releaseCommand） |
| **输入输出** | **命令**：`alembic upgrade head`<br>**输出**：`Running upgrade 0004 -> 0005` |
| **关键配置** | 最新迁移：`20260712_0005_add_user_type.py` |

### 7.7 备份恢复

| 项目 | 说明 |
|------|------|
| **功能名称** | 数据库备份恢复 |
| **功能作用** | 数据库备份和恢复脚本 |
| **后端实现** | 脚本：`backend/scripts/backup_db.sh` + `restore_db.sh` |
| **前端实现** | 无独立 UI |
| **技术栈** | pg_dump + shell |
| **核心逻辑** | 1. pg_dump 导出数据库<br>2. 支持全量和增量备份<br>3. restore_db.sh 从备份文件恢复 |
| **输入输出** | **输入**：数据库连接信息<br>**输出**：备份文件（.sql） |
| **关键配置** | `DATABASE_URL` |

---

## 8. 前端基础设施

### 8.1 路由与代码分割

| 项目 | 说明 |
|------|------|
| **功能名称** | React 路由 + 代码分割 |
| **功能作用** | React Router 路由管理，React.lazy + Suspense 代码分割 |
| **后端实现** | 无（纯前端） |
| **前端实现** | 库：react-router-dom v7；配置：`src/App.tsx`；路由守卫：`AuthGuard` 组件 |
| **技术栈** | React Router v7 + React.lazy + Suspense + Vite chunk 分割 |
| **核心逻辑** | 1. 除 LoginPage/NotFoundPage 外所有页面用 React.lazy() 动态导入<br>2. Suspense 包裹路由，loading 时显示骨架屏<br>3. 受保护路由用 AuthGuard 组件<br>4. Vite 自动分割 chunk<br>5. `lucide-react` 独立为 `icons-vendor` chunk |
| **输入输出** | **输入**：URL 路径<br>**输出**：对应页面组件 |
| **关键配置** | Vite 配置：`lucide-react` 独立 chunk |

### 8.2 状态管理（Zustand）

| 项目 | 说明 |
|------|------|
| **功能名称** | 全局状态管理 |
| **功能作用** | 全局状态管理（认证状态、文档状态、对话状态等） |
| **后端实现** | 无（纯前端） |
| **前端实现** | 库：Zustand v5；Store：`src/store/authStore.ts`、`documentStore.ts`、`toastStore.ts` |
| **技术栈** | Zustand v5 + localStorage 持久化 |
| **核心逻辑** | 1. authStore：token 持久化、用户信息、登录/登出/guestLogin<br>2. documentStore：文档列表、分支、多选状态、排序、搜索、轮询<br>3. toastStore：全局通知（success/error/warning/info）<br>4. 持久化：localStorage 存储 token 和搜索历史 |
| **输入输出** | **输入**：action 调用<br>**输出**：状态更新 |
| **关键配置** | 无 |

### 8.3 API 封装

| 项目 | 说明 |
|------|------|
| **功能名称** | 统一 API 请求封装 |
| **功能作用** | 统一的 API 请求封装，自动携带 Token、错误处理、401 刷新 |
| **后端实现** | 无（纯前端） |
| **前端实现** | 模块：`src/api/client.ts`（axios 封装）+ `src/api/*.ts`（各业务 API）；常量：`src/utils/constants.ts`（API_PATHS） |
| **技术栈** | axios + 请求/响应拦截器 |
| **核心逻辑** | 1. 请求拦截器：自动添加 `Authorization: Bearer <token>`<br>2. 响应拦截器：401 时自动刷新 Token<br>3. API 路径常量集中管理<br>4. 业务 API：auth.ts / chat.ts / document.ts |
| **输入输出** | **输入**：API 请求参数<br>**输出**：Promise<响应数据> |
| **关键配置** | `API_BASE_URL`、Token 刷新逻辑 |

### 8.4 对话界面

| 项目 | 说明 |
|------|------|
| **功能名称** | 聊天界面（流式 + Markdown + 公式） |
| **功能作用** | 聊天界面，支持流式输出、Markdown 渲染、LaTeX 公式、引用来源展示 |
| **后端实现** | 路由：`chat.py` ask_question_stream（SSE 流式） |
| **前端实现** | 页面：`src/pages/ChatPage.tsx`；组件：`MessageBubble.tsx`、`MarkdownRenderer.tsx`、`SourceCard.tsx`；API：`src/api/chat.ts` |
| **技术栈** | React + fetch stream + KaTeX + 代码高亮 |
| **核心逻辑** | 1. SSE 流式接收<br>2. Markdown 渲染（代码高亮）<br>3. LaTeX 公式渲染（KaTeX）<br>4. 引用来源卡片展示（含置信度标签）<br>5. 多对话切换（侧边栏对话列表） |
| **输入输出** | **输入**：用户输入文本<br>**输出**：流式回答 + 引用来源 |
| **关键配置** | 无 |

### 8.5 文档管理界面

| 项目 | 说明 |
|------|------|
| **功能名称** | 文档管理界面（三栏布局） |
| **功能作用** | 文档列表、上传、预览、重命名、删除、分支管理、多选批量操作 |
| **后端实现** | 路由：`documents.py` + `folders.py`（提供全部 API） |
| **前端实现** | 页面：`src/pages/DocumentsPage.tsx`；组件：`DocumentList.tsx`、`DocumentItem.tsx`、`Sidebar.tsx`、`SortDropdown.tsx`、`BatchActionBar.tsx`、`BatchMoveModal.tsx`、`MoveToFolderModal.tsx` |
| **技术栈** | React + Zustand + TailwindCSS + lucide-react |
| **核心逻辑** | 1. 三栏布局：侧边栏 + 文档列表 + 预览面板<br>2. 文档列表分页展示 + 排序 + 搜索<br>3. 拖拽上传 + URL 导入<br>4. 文档预览面板（文本/PDF/DOCX）<br>5. 分支管理 + 未分类虚拟分支<br>6. **多选模式**：复选框 + 批量操作工具栏<br>7. 侧边栏可见性由父容器 `hidden lg:block` 控制 |
| **输入输出** | **输入**：用户操作<br>**输出**：文档列表更新 |
| **关键配置** | 侧边栏 w-60 固定宽度、多选模式 selectionMode |

### 8.6 管理员审批界面

| 项目 | 说明 |
|------|------|
| **功能名称** | 管理员注册审批 |
| **功能作用** | 管理员审批用户注册申请 |
| **后端实现** | 路由：`registration.py`（审批接口） |
| **前端实现** | 页面：`src/pages/AdminApplicationsPage.tsx` |
| **技术栈** | React + FastAPI |
| **核心逻辑** | 1. 申请列表展示<br>2. 批准/拒绝操作<br>3. 批准后触发密码设置邮件 |
| **输入输出** | **输入**：审批决策<br>**输出**：申请状态更新 |
| **关键配置** | 仅管理员可访问 |

### 8.7 品牌与主题一致性

| 项目 | 说明 |
|------|------|
| **功能名称** | 品牌视觉一致性 |
| **功能作用** | 统一的品牌色调（#D97757）和视觉风格 |
| **后端实现** | 无（纯前端） |
| **前端实现** | TailwindCSS 配置 + `favicon.svg` + `manifest.json` + `ThemeToggle` 组件 |
| **技术栈** | TailwindCSS + SVG + manifest.json |
| **核心逻辑** | 1. 品牌色 #D97757 贯穿 UI<br>2. favicon.svg 与品牌色一致<br>3. manifest.json theme_color 与品牌色一致<br>4. 项目名称"GeiIt企业知识库"统一<br>5. 暗色模式切换 |
| **输入输出** | 无 |
| **关键配置** | TailwindCSS 配色、favicon.svg、manifest.json theme_color |

---

## 附录：技术栈版本

| 类别 | 技术 | 版本/说明 |
|------|------|-----------|
| 后端框架 | FastAPI | 异步 Web 框架 |
| 数据库 | PostgreSQL + pgvector | pg16 + vector 扩展 |
| 缓存 | Redis 7 | AOF 持久化 |
| 任务队列 | Celery 5.4+ | broker_connection_retry_on_startup=True |
| ORM | SQLAlchemy 2.0 | Mapped 类型注解 |
| 迁移工具 | Alembic | 5 个迁移版本 |
| LLM 框架 | LangChain | Prompt 模板 + 文本分割器 |
| Embedding | 阿里云 text-embedding-v4 | 1024 维（传 dimensions=1536） |
| LLM | DeepSeek（主）+ OpenAI 兼容 | chat/completions 端点 |
| Reranker | BAAI/bge-reranker-base | cross-encoder |
| 前端框架 | React 18 + TypeScript | |
| 构建工具 | Vite | 代码分割 + chunk 优化 |
| 路由 | react-router-dom v7 | |
| 状态管理 | Zustand v5 | |
| 样式 | TailwindCSS | |
| 邮件服务 | Resend | SMTP smtp.resend.com:465 |
| 部署 | Docker Compose（本地）/ Railway（生产） | |

---

## 附录：本次更新内容（2026-07-13）

### 新增功能

1. **文档排序修复**（2.3）：后端 `list_documents` 新增 `sort_by`/`sort_order` 参数，支持按创建时间/修改时间/文件名/文件类型排序，白名单映射防 SQL 注入。
2. **未分类文档视图**（2.10）：folder_id=0 哨兵值表示"未分类"虚拟分支，侧边栏新增"未分类"入口，后端 `folder_id IS NULL` 过滤。
3. **文档多选与批量操作**（2.11）：新增 `batch-delete`/`batch-move` API，前端 `BatchActionBar`/`BatchMoveModal` 组件，支持全选/批量删除/批量移动。

### 文档结构调整

- 每个功能项整合了**后端实现**和**前端实现**，便于分块检索时一次性获取完整信息。
- 新增"技术栈"字段，标注每个功能使用的技术。
- 功能编号保持一致，新增功能标注 ★ 符号。
