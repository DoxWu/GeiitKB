# GeiIt企业知识库 - 前端环境变量示例
# 复制此文件为 .env.local（开发）或在 Railway 中配置（生产）

# API 基础地址（包含 /api/v1 路径前缀）
# 开发环境（直连后端）：http://localhost:8000/api/v1
# Railway 生产环境（反代模式，推荐）：/api/v1（需配合 nginx 反向代理 + BACKEND_URL 环境变量）
# Railway 生产环境（跨域模式，已废弃）：https://your-backend.up.railway.app/api/v1
VITE_API_BASE_URL=http://localhost:8000/api/v1

# 应用标题
VITE_APP_TITLE=GeiIt企业知识库

# ============================================
# Railway 部署说明
# ============================================
# 推荐使用反向代理模式（解决国产浏览器兼容性问题）：
#
# 前端服务环境变量：
#   - VITE_API_BASE_URL=/api/v1（同源访问，由 nginx 转发）
#   - BACKEND_URL=https://your-backend.up.railway.app（后端公网地址，注入到 nginx）
#
# 后端服务环境变量：
#   - CORS_ORIGINS 仍需配置前端域名（开发环境调试用，生产环境不触发 CORS）
#
# 优势：
#   - 无 CORS 跨域问题，消除 OPTIONS 预检请求
#   - 浏览器只访问前端域名，不直连后端，解决国产浏览器网络兼容性
#   - 服务端转发比浏览器直连海外服务器更稳定
