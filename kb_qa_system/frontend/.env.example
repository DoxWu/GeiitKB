# GeiIt企业知识库 - 前端环境变量示例
# 复制此文件为 .env.local（开发）或在 Railway 中配置（生产）

# API 基础地址（包含 /api/v1 路径前缀）
# 开发环境：http://localhost:8000/api/v1
# Railway 生产环境（跨域模式）：https://your-backend.up.railway.app/api/v1
# Railway 生产环境（反代模式）：/api/v1（需配合 nginx 反向代理）
VITE_API_BASE_URL=http://localhost:8000/api/v1

# 应用标题
VITE_APP_TITLE=GeiIt企业知识库

# ============================================
# Railway 部署说明
# ============================================
# 1. 跨域模式（推荐）：
#    - VITE_API_BASE_URL 设为后端 Railway 公网 URL + /api/v1
#    - 后端 CORS_ORIGINS 添加前端 Railway 公网 URL
#    - 前后端独立部署，各自有公网域名
#
# 2. 反向代理模式（可选）：
#    - VITE_API_BASE_URL 设为 /api/v1（同源）
#    - nginx.conf 取消注释 /api/ location 块
#    - 配置 BACKEND_URL 环境变量指向后端内部地址
#    - 优势：无 CORS 问题，前端请求同源
