# GeiIt企业知识库 — 10D 审查报告 29 项改进系统性优化实施计划

> 创建日期：2026-07-11
> 来源：`docs/COMPREHENSIVE_REVIEW_10D.md`（P1×4, P2×14, P3×11，共 29 项）
> 目标：完成全部 29 项改进，通过回归测试，形成改进实施报告

---

## 一、改进项分类总览

### A. 功能性改进（6 项）
| 编号 | 级别 | 描述 | 现状 |
|------|------|------|------|
| D1-01 | P1 | 文档库分支 Mock → 真实后端 CRUD | document.ts 用 localStorage Mock，后端无端点，Document 模型无 folder_id |
| D1-02 | P3 | auth.ts 顶部注释过时 | L6 称"Mock 实现"但实际已真实 API |
| D10-01 | P1 | 无操作审计日志 | 无 audit_logs 表/模型 |
| D10-02 | P2 | 无帮助文档页面 | App.tsx 无 /help 路由 |
| D10-04 | P3 | Cookie/Token 说明 | PrivacyPage 已有章节六，小幅增强 |
| D4-01 | P1 | 负载测试 | ✅ 已完成（locustfile.py 已创建） |

### B. 性能优化（5 项）
| 编号 | 级别 | 描述 | 现状 |
|------|------|------|------|
| D2-01 | P2 | 文档搜索用 LIKE | documents.py list_documents 无 search 参数（前端传了但后端忽略） |
| D4-02 | P2 | offset 分页性能 | documents.py L447、chat.py L721 用 offset/limit |
| D4-03 | P2 | 文档列表 N+1 | DocumentResponse 无 user 关联字段，实际无 N+1（验证即可） |
| D4-04 | P3 | IVFFlat lists 固定 | 无运行时 probes 调优配置 |

### C. 代码质量提升（3 项）
| 编号 | 级别 | 描述 | 现状 |
|------|------|------|------|
| D6-02 | P3 | Celery 无死信队列 | celery_app.py 4 队列无 DLQ |
| D8-02 | P3 | X-Request-ID 未传递 | CORS expose 含 X-Request-ID 但无中间件生成 |
| D7-03 | P3 | 无在线迁移策略 | 无文档 |

### D. 安全性增强（4 项）
| 编号 | 级别 | 描述 | 现状 |
|------|------|------|------|
| D3-01 | P2 | Token 存 localStorage | client.ts L45/L57，需加 CSP 降低 XSS 风险 |
| D3-02 | P2 | /metrics 默认无认证 | PROMETHEUS_AUTH_ENABLED 默认 False |
| D6-01 | P2 | Sentry 未初始化 | main.py 无 SDK init 代码 |
| D10-03 | P2 | 数据保留策略未定义 | 无配置项和清理任务 |

### E. 用户体验优化（6 项）
| 编号 | 级别 | 描述 | 现状 |
|------|------|------|------|
| D5-01 | P2 | IME 兼容性 | SearchBar/ChatInput 无 composition 事件处理 |
| D5-02 | P2 | 移动端上传 | UploadZone 已支持点击上传，需优化触摸目标和响应式 |
| D5-03 | P3 | 无 PWA | 无 manifest.json / service worker |
| D5-04 | P3 | 无暗色模式 | tailwind 已配 darkMode:"class"，无切换和 dark: 样式 |
| D2-02 | P3 | 搜索结果无高亮 | SearchBar 无高亮逻辑 |
| D2-03 | P3 | 无搜索历史 | 无搜索历史功能 |

### F. 运维/部署改进（6 项）
| 编号 | 级别 | 描述 | 现状 |
|------|------|------|------|
| D7-01 | P1 | 无备份脚本 | scripts/ 无 backup_db.sh |
| D7-02 | P2 | 无恢复验证 | scripts/ 无 verify_backup.sh |
| D8-01 | P2 | Alertmanager 未配置 | monitoring/ 仅 Prometheus+Grafana |
| D9-01 | P2 | 多副本迁移冲突 | entrypoint.sh 启动时迁移，railway.json 无 releaseCommand |
| D9-02 | P2 | 回滚 SOP | 无文档 |
| D9-03 | P3 | 无灰度发布 | 无文档/配置 |

---

## 二、实施计划（按依赖顺序分 4 个阶段）

### 阶段 1：后端核心改进（数据模型 + API + 中间件）

#### 1.1 D10-01 审计日志（P1）
**文件**：
- 新建 `backend/app/models/audit_log.py` — AuditLog 模型
- 新建 `backend/alembic/versions/20260711_0004_add_audit_logs.py` — 迁移
- 新建 `backend/app/services/audit_service.py` — 审计服务
- 修改 `backend/app/models/__init__.py` — 导出 AuditLog
- 修改 `backend/app/api/routes/documents.py` — 删除文档时记录审计
- 修改 `backend/app/api/routes/registration.py` — 审批操作记录审计
- 修改 `backend/app/api/routes/auth.py` — 删除账户记录审计

**AuditLog 模型设计**：
```python
class AuditLog(Base):
    __tablename__ = "audit_logs"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    user_id: Mapped[Optional[int]] = mapped_column(Integer, ForeignKey("users.id", ondelete="SET NULL"), nullable=True, index=True)
    action: Mapped[str] = mapped_column(String(50), index=True)  # document.delete/account.delete/application.approve等
    resource_type: Mapped[str] = mapped_column(String(50), index=True)  # document/user/application
    resource_id: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)
    detail: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True)  # 附加信息
    ip_address: Mapped[Optional[str]] = mapped_column(String(45), nullable=True)
    user_agent: Mapped[Optional[str]] = mapped_column(String(500), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), index=True)
```

**审计服务**：`audit_service.log(db, user_id, action, resource_type, resource_id, detail, request)` — 从 request 提取 IP/UA

**集成点**：
- 文档删除（documents.py delete_document）
- 账户删除（auth.py delete_account）
- 注册审批/拒绝（registration.py approve/reject）
- 文档可见性变更（documents.py upload public）
- 超管跨用户操作（已有 _audit_superuser_action，改用统一服务）

#### 1.2 D1-01 文档库分支 CRUD（P1）
**文件**：
- 新建 `backend/app/models/document_folder.py` — DocumentFolder 模型
- 修改 `backend/alembic/versions/20260711_0004_add_audit_logs.py` — 同时建 folders 表 + Document.folder_id 列
- 新建 `backend/app/schemas/document_folder.py` — Folder Schema
- 修改 `backend/app/api/routes/documents.py` — 新增 5 个 folder 端点
- 修改 `backend/app/models/document.py` — 添加 folder_id 字段
- 修改 `backend/app/models/__init__.py` — 导出 DocumentFolder
- 修改 `frontend/src/api/document.ts` — 移除 Mock，改用真实 API
- 修改 `frontend/src/types/document.ts` — 确认 DocumentFolder 类型

**DocumentFolder 模型**：
```python
class DocumentFolder(Base):
    __tablename__ = "document_folders"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    name: Mapped[str] = mapped_column(String(100))  # 分支名
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.id", ondelete="CASCADE"), index=True)  # 所属用户
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())
    # 唯一约束：同一用户下分支名唯一
    __table_args__ = (UniqueConstraint("user_id", "name", name="uq_folder_user_name"),)
```

**Document 模型新增**：`folder_id: Mapped[Optional[int]] = mapped_column(Integer, ForeignKey("document_folders.id", ondelete="SET NULL"), nullable=True, index=True)`

**5 个端点**（挂载在 /documents/folders）：
1. `GET /documents/folders` — 列出当前用户的分支（含 document_count）
2. `POST /documents/folders` — 创建分支
3. `PATCH /documents/folders/{id}` — 重命名
4. `DELETE /documents/folders/{id}` — 删除分支（文档 folder_id 置 NULL）
5. `GET /documents/folders/{id}/documents` — 分支内文档列表

**前端改动**：document.ts 移除 readMockFolders/writeMockFolders/mockDelay 和 4 个 Mock 函数体，改为调用 `apiClient.get/post/patch/delete`，API_PATHS 已定义 FOLDERS/FOLDER_DETAIL。

#### 1.3 D2-01 全文检索替代 LIKE（P2）
**文件**：修改 `backend/app/api/routes/documents.py` list_documents

**改动**：
1. 新增 `search: Optional[str] = Query(None, max_length=200)` 参数
2. 搜索逻辑改用 PostgreSQL 全文检索：
```python
if search:
    search_term = search.strip()
    if search_term:
        # 使用 to_tsvector + @@ 操作符，利用已有 GIN 索引
        query = query.filter(
            func.to_tsvector('simple', Document.title).match(search_term)
            | Document.title.ilike(f"%{search_term}%")  # 降级兼容短词
        )
```
3. 前端 document.ts 已传 search 参数（L43），无需前端改动

#### 1.4 D4-02 游标分页（P2）— 向后兼容
**文件**：修改 `backend/app/api/routes/documents.py`、`backend/app/api/routes/chat.py`、对应 schema

**策略**：新增可选 `cursor` 参数，保持 `page/page_size` 向后兼容
- 无 cursor 时：用 offset/limit（兼容旧前端）
- 有 cursor 时：用 `WHERE id < cursor ORDER BY id DESC LIMIT page_size`
- 响应新增 `next_cursor: Optional[int]` 字段（旧前端忽略）

**documents.py list_documents 改动**：
```python
cursor: Optional[int] = Query(None, ge=1, description="游标（上一页最后一条文档ID）"),
# ...
if cursor:
    query = query.filter(Document.id < cursor)
    documents = query.order_by(Document.id.desc()).limit(page_size).all()
    total = query.count()  # 仅无游标时返回总数
else:
    # 原 offset 逻辑保持
```
DocumentListResponse 新增 `next_cursor: Optional[int] = None`

**chat.py list_conversations** 同理，用 Conversation.id 作游标

#### 1.5 D4-03 N+1 验证（P2）
**结论**：DocumentResponse schema 不含 user 关联字段（无 owner_name/user 对象），list_documents 查询不访问任何 relationship，**无 N+1 问题**。
**行动**：在 documents.py list_documents 添加注释说明已验证无 N+1，无需代码改动。在改进报告中记录验证结论。

#### 1.6 D3-02 /metrics 生产强制认证（P2）
**文件**：修改 `backend/app/core/config.py`

**改动**：在 `validate_required_for_production` 方法中添加校验：
```python
if settings.ENVIRONMENT == "production":
    if not settings.PROMETHEUS_AUTH_ENABLED:
        errors.append("生产环境必须开启 PROMETHEUS_AUTH_ENABLED")
    if not settings.PROMETHEUS_AUTH_PASSWORD:
        errors.append("生产环境必须配置 PROMETHEUS_AUTH_PASSWORD")
```
需先确认 validate_required_for_production 方法位置和现有校验模式。

#### 1.7 D6-01 Sentry SDK 初始化（P2）
**文件**：修改 `backend/app/main.py`、`backend/requirements.txt`

**改动**：
1. requirements.txt 添加 `sentry-sdk[fastapi]>=1.40.0`
2. main.py lifespan 启动时初始化（在配置校验后）：
```python
if settings.ENABLE_SENTRY and settings.SENTRY_DSN:
    import sentry_sdk
    from sentry_sdk.integrations.fastapi import FastApiIntegration
    from sentry_sdk.integrations.celery import CeleryIntegration
    from sentry_sdk.integrations.redis import RedisIntegration

    sentry_sdk.init(
        dsn=settings.SENTRY_DSN,
        environment=settings.ENVIRONMENT,
        traces_sample_rate=0.1,
        send_default_pii=False,  # PII 过滤
        integrations=[
            FastApiIntegration(),
            CeleryIntegration(),
            RedisIntegration(),
        ],
        before_send=_filter_pii,  # 自定义 PII 过滤函数
    )
```
3. 添加 `_filter_pii` 函数：过滤 event 中的 password/token/secret 字段

#### 1.8 D6-02 Celery 死信队列（P3）
**文件**：修改 `backend/app/core/celery_app.py`

**改动**：
1. 新增死信队列 `Queue("dead_letter", routing_key="dead_letter.#")`
2. 配置 `task_reject_on_worker_lost = True`
3. 添加死信路由：失败任务达到 max_retries 后路由到 dead_letter 队列
4. 新建 `backend/app/tasks/dlq_handler.py` — 定时扫描死信队列的 beat 任务（可选，先记录日志）

```python
task_queues=(
    Queue("default", routing_key="default.#"),
    Queue("document", routing_key="document.#"),
    Queue("embedding", routing_key="embedding.#"),
    Queue("email", routing_key="email.#"),
    Queue("dead_letter", routing_key="dead_letter.#"),  # 新增
),
```

#### 1.9 D8-02 X-Request-ID 中间件（P3）
**文件**：新建 `backend/app/middleware/request_id_middleware.py`、修改 `backend/app/main.py`

**中间件逻辑**：
1. 从请求头读取 `X-Request-ID`，没有则用 `uuid.uuid4().hex` 生成
2. 写入响应头 `X-Request-ID`
3. 注入 structlog contextvars（`structlog.contextvars.bind_contextvars(request_id=...)`）
4. 请求结束后清除 contextvars

**main.py 注册**：在 CORS 之后、Prometheus 之前添加：
```python
from app.middleware.request_id_middleware import RequestIDMiddleware
app.add_middleware(RequestIDMiddleware)
```

#### 1.10 D10-03 数据保留策略（P2）
**文件**：修改 `backend/app/core/config.py`、新建 `backend/app/tasks/cleanup_tasks.py`、修改 `backend/app/core/celery_app.py`

**config.py 新增**：
```python
# 数据保留策略
CONVERSATION_RETENTION_DAYS: int = 90  # 对话历史保留天数
AUDIT_LOG_RETENTION_DAYS: int = 365    # 审计日志保留天数
QA_EVENT_RETENTION_DAYS: int = 90      # QA事件保留天数
EMAIL_LOG_RETENTION_DAYS: int = 30     # 邮件日志保留天数
CLEANUP_ENABLED: bool = True           # 是否启用自动清理
```

**cleanup_tasks.py**：Celery 定时任务，每日凌晨执行：
- 删除超过保留期的已软删除对话（is_active=False）
- 删除超过保留期的 QAEvent
- 删除超过保留期的 EmailLog
- 删除超过保留期的 AuditLog（除关键操作外）

**celery_app.py**：配置 beat_schedule 每日 03:00 执行清理

#### 1.11 D4-04 IVFFlat probes 调优（P3）
**文件**：修改 `backend/app/core/config.py`、`backend/app/services/vector_store.py`

**改动**：
1. config.py 新增 `IVFFLAT_PROBES: int = 10`
2. vector_store.py 检索前执行 `SET LOCAL ivfflat.probes = :probes`（在事务内）
3. 添加注释说明数据量增长后需重建索引调整 lists

---

### 阶段 2：前端改进（组件 + 页面 + 样式）

#### 2.1 D5-01 IME 兼容性（P2）
**文件**：修改 `SearchBar.tsx`、`ChatInput.tsx`

**SearchBar.tsx 改动**：
```tsx
const [isComposing, setIsComposing] = useState(false);
// input 添加：
onCompositionStart={() => setIsComposing(true)}
onCompositionEnd={() => { setIsComposing(false); }}
// useEffect 依赖条件改为：
if (!isComposing) {
  timerRef.current = setTimeout(() => loadDocuments(), DEBOUNCE_DELAY);
}
```

**ChatInput.tsx 改动**：
```tsx
const isComposingRef = useRef(false);
// textarea 添加：
onCompositionStart={() => { isComposingRef.current = true; }}
onCompositionEnd={() => { isComposingRef.current = false; }}
// handleKeyDown 修改：
if (e.key === "Enter" && !e.shiftKey && !isComposingRef.current) {
  e.preventDefault();
  handleSend();
}
```

#### 2.2 D5-02 移动端上传优化（P2）
**文件**：修改 `UploadZone.tsx`

**改动**：
1. 拖拽区添加 `min-h-[120px]` 和 `touch-manipulation` 类优化触摸目标
2. 按钮添加 `min-h-[44px]`（iOS HIG 最小触摸目标）
3. 检测移动端（`pointer: coarse`）时隐藏拖拽提示文案，只显示"点击上传"
4. 添加 `role="button"` 和 `tabIndex={0}` 增强可访问性

#### 2.3 D5-03 PWA 支持（P3）
**文件**：新建 `frontend/public/manifest.json`、新建 `frontend/public/sw.js`、修改 `frontend/index.html`、修改 `frontend/vite.config.ts`

**manifest.json**：应用名、图标、主题色、显示模式
**sw.js**：缓存静态资源（stale-while-revalidate 策略），离线回退到 index.html
**vite.config.ts**：注册 VitePWA 插件（或手动注册 SW）

**注**：由于项目未安装 vite-plugin-pwa，采用手动 SW 注册方式，避免新增依赖。

#### 2.4 D5-04 暗色模式（P3）
**文件**：新建 `frontend/src/store/themeStore.ts`、新建 `frontend/src/components/common/ThemeToggle.tsx`、修改 `frontend/src/main.tsx`、修改 `frontend/tailwind.config.js`、修改主要页面/组件

**策略**：
1. themeStore（Zustand）：`theme: 'light' | 'dark' | 'system'`，持久化到 localStorage
2. ThemeToggle 组件：三态切换按钮（太阳/月亮/系统图标）
3. main.tsx：启动时根据 theme 给 `<html>` 添加/移除 `dark` class
4. tailwind.config.js：扩展暗色色板（在现有 colors 基础上添加 dark 变体）
5. 关键组件添加 `dark:` 样式：Layout、DocumentsPage、ChatPage、LoginPage、Sidebar、Navbar
6. CSS 变量方案：在 `index.css` 定义 `:root` 和 `.dark` 两套 CSS 变量，组件引用变量名

**范围**：核心页面（登录、文档、聊天、设置）+ 公共组件（Layout、Button、Input、Card、Modal、Toast）。非核心页面使用 CSS 变量自动适配。

#### 2.5 D2-02 搜索结果高亮（P3）
**文件**：修改 `frontend/src/components/documents/DocumentList.tsx`（或 DocumentCard.tsx）、新建 `frontend/src/utils/highlight.ts`

**highlight.ts**：
```tsx
export function highlightText(text: string, keyword: string): React.ReactNode {
  if (!keyword.trim()) return text;
  const parts = text.split(new RegExp(`(${escapeRegExp(keyword)})`, 'gi'));
  return parts.map((part, i) =>
    part.toLowerCase() === keyword.toLowerCase()
      ? <mark key={i} className="bg-yellow-200 text-ink">{part}</mark>
      : part
  );
}
```
在文档列表项的 title 中调用 highlightText(title, searchKeyword)

#### 2.6 D2-03 搜索历史（P3）
**文件**：修改 `frontend/src/store/documentStore.ts`、修改 `SearchBar.tsx`

**documentStore.ts 新增**：
```tsx
searchHistory: string[];  // 最多保留 10 条
addSearchHistory: (keyword: string) => void;
clearSearchHistory: () => void;
```
持久化到 localStorage（key: `kb_search_history`）

**SearchBar.tsx**：
- 输入框聚焦时，若有历史记录则下拉显示
- 点击历史项直接搜索
- 清除历史按钮

#### 2.7 D3-01 CSP 策略（P2）
**文件**：修改 `frontend/nginx.conf`

**改动**：添加 Content-Security-Policy 头：
```nginx
add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' https:; frame-ancestors 'self'; base-uri 'self'; form-action 'self';" always;
```
**说明**：选择 CSP 而非 HttpOnly Cookie，因为 HttpOnly Cookie 需重构整个认证流程（client.ts/authStore/所有 API 调用），CSP 能有效降低 XSS 风险且不破坏现有架构。在改进报告中记录 HttpOnly Cookie 作为未来增强方向。

#### 2.8 D10-02 帮助文档页面（P2）
**文件**：新建 `frontend/src/pages/HelpPage.tsx`、修改 `frontend/src/App.tsx`、修改导航组件

**HelpPage 内容**：
1. 快速开始：注册→登录→上传文档→提问
2. 文档管理：上传、URL导入、分支管理、删除
3. 问答功能：流式输出、引用来源、多轮对话
4. 常见问题 FAQ（8-10 条）
5. 快捷键说明

**App.tsx**：添加 `/help` 路由（受保护页面）
**导航**：在侧边栏/顶部导航添加"帮助"链接

#### 2.9 D1-02 auth.ts 注释修正（P3）
**文件**：修改 `frontend/src/api/auth.ts`

L6 注释从：
```
*   注册申请和密码设置接口需后端新增，当前使用 Mock 实现。
```
改为：
```
*   注册审批流程接口已由后端实现（registration.py），以下为真实 API 调用。
```

#### 2.10 D10-04 隐私政策增强（P3）
**文件**：修改 `frontend/src/pages/PrivacyPage.tsx`

PrivacyPage 已有"六、Cookie 与 Token 说明"章节。增强内容：
1. 补充 CSP 策略说明（新增）
2. 补充数据保留期限的具体天数（对接 D10-03 配置：对话 90 天、审计日志 365 天等）
3. 更新"最后更新"日期为 2026年7月11日

---

### 阶段 3：基础设施改进（脚本 + 配置 + 文档）

#### 3.1 D7-01 数据库备份脚本（P1）
**文件**：新建 `backend/scripts/backup_db.sh`

**功能**：
- 从 DATABASE_URL 解析连接参数
- pg_dump 导出（自定义格式，支持并行恢复）
- 按日期命名 `backup_YYYYMMDD_HHMMSS.dump`
- 保留最近 N 份（默认 30），自动清理旧备份
- 可配置：`BACKUP_DIR`、`BACKUP_RETENTION_DAYS`
- 输出备份大小和耗时日志

#### 3.2 D7-02 恢复验证脚本（P2）
**文件**：新建 `backend/scripts/verify_backup.sh`

**功能**：
- 参数：备份文件路径
- 创建临时数据库（或使用 BACKUP_VERIFY_DB）
- pg_restore 恢复到临时库
- 校验：关键表行数、最新记录时间戳、向量索引存在性
- 输出验证报告
- 清理临时数据库

#### 3.3 D8-01 Alertmanager 配置（P2）
**文件**：新建 `monitoring/alertmanager.yml`、新建 `monitoring/alertmanager_templates/`、修改 `monitoring/docker-compose.monitoring.yml`、修改 `monitoring/prometheus.yml`

**alertmanager.yml**：
- 邮件通知渠道（复用 SMTP 配置）
- 告警路由规则：critical → 立即邮件；warning → 5分钟未恢复才邮件
- 告警分组：按服务名分组，避免告警风暴
- 抑制规则：critical 触发时抑制同级 warning

**docker-compose.monitoring.yml**：添加 alertmanager 服务
**prometheus.yml**：添加 `alerting.alertmanagers` 配置

#### 3.4 D9-01 releaseCommand 配置（P2）
**文件**：修改 `backend/railway.json`、修改 `backend/entrypoint.sh`

**railway.json**：
```json
{
  "deploy": {
    "startCommand": "/app/entrypoint.sh",
    "releaseCommand": "alembic upgrade head",
    ...
  }
}
```

**entrypoint.sh**：
- start_api() 中移除 `run_migrations` 调用
- 保留 run_migrations 函数但注释说明"由 Railway releaseCommand 执行"
- 添加检测：若 MIGRATE_ON_STARTUP=true 环境变量为真才执行（兼容非 Railway 部署）

#### 3.5 D9-02 回滚 SOP 文档（P2）
**文件**：新建 `docs/ROLLBACK_SOP.md`

**内容**：
1. 回滚决策流程图：代码回滚 vs 数据库回滚的判断标准
2. Railway Dashboard 代码回滚步骤
3. Alembic downgrade 流程和注意事项
4. 回滚后验证清单
5. 紧急回滚联系人/通讯录模板
6. 常见回滚场景和处置方案

#### 3.6 D9-03 灰度发布方案（P3）
**文件**：新建 `docs/CANARY_RELEASE.md`

**内容**：
1. Railway 多 environment 方案：production + staging
2. 域名权重分流方案（nginx upstream 权重）
3. 数据库共享 vs 隔离的权衡
4. 灰度验证清单
5. 全量发布步骤

#### 3.7 D7-03 在线迁移策略（P3）
**文件**：新建 `docs/ONLINE_MIGRATION_GUIDE.md`

**内容**：
1. 在线迁移三步法：扩展列 → 回填数据 → 收缩/切换
2. pgvector 索引重建策略（CONCURRENTLY）
3. 大表迁移注意事项（锁、超时、批处理）
4. Alembic 在线迁移代码示例

---

### 阶段 4：回归测试 + 改进实施报告

#### 4.1 回归测试
1. **后端测试**：`pytest`（现有 38 passed + 新增审计日志/分支 CRUD 测试）
2. **前端 TypeScript**：`node node_modules/typescript/bin/tsc --noEmit`（0 errors）
3. **前端单元测试**：`npm.cmd test`（现有 473 passed + 新增组件测试）
4. **前端构建**：`npm.cmd run build`（成功）
5. **迁移验证**：`alembic upgrade head` + `alembic downgrade -1` + `alembic upgrade head`（可逆性）
6. **新增测试**：
   - 审计日志模型/服务测试
   - 文档分支 CRUD 端点测试
   - 全文检索测试
   - 游标分页测试
   - IME 组件测试
   - 暗色模式主题切换测试

#### 4.2 改进实施报告
**文件**：新建 `docs/IMPROVEMENT_IMPLEMENTATION_REPORT.md`

**内容**：
1. 29 项改进落实情况汇总表（编号/级别/类别/状态/修改文件）
2. 各项改进的详细实施记录
3. 遇到的问题及解决方案
4. 回归测试结果
5. 遗留事项和后续建议
6. 与 10D 审查报告的对比（改进前 vs 改进后评分预估）

---

## 三、关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| D3-01 Token 存储 | CSP 策略（非 HttpOnly Cookie） | HttpOnly Cookie 需重构整个认证流程，CSP 有效降低 XSS 风险且不破坏现有架构 |
| D4-02 游标分页 | 可选 cursor 参数，向后兼容 | 不破坏现有 API 契约，前端可渐进迁移 |
| D5-04 暗色模式 | CSS 变量 + 核心页面 dark: 样式 | 全组件 dark: 改动量过大，CSS 变量让非核心页面自动适配 |
| D5-03 PWA | 手动 SW 注册（非 vite-plugin-pwa） | 避免新增依赖，保持构建配置简洁 |
| D1-01 分支模型 | DocumentFolder 独立表 + Document.folder_id 外键 | 比存 metadata_ JSON 更规范，支持外键约束和索引 |
| D10-01 审计日志 | 独立 audit_logs 表 + audit_service 统一服务 | 集中管理，便于查询和保留策略 |

---

## 四、预期成果

1. **29 项改进全部落实**（D4-01 已完成，D4-03 经验证无需改动，其余 27 项实施）
2. **新增/修改文件**：
   - 后端：~15 个文件（模型 2、迁移 1、服务 2、路由修改 4、中间件 1、配置 2、任务 1、脚本 2）
   - 前端：~12 个文件（新页面 2、新组件 2、新工具 2、修改组件 6）
   - 基础设施：~8 个文件（监控 3、文档 4、配置 1）
   - 报告：1 个（改进实施报告）
3. **回归测试全部通过**
4. **改进实施报告**记录全部落实情况

---

## 五、验证步骤

1. 后端：`cd kb_qa_system/backend && python -m pytest` — 全部通过
2. 前端 TS：`cd kb_qa_system/frontend && node node_modules/typescript/bin/tsc --noEmit` — 0 errors
3. 前端测试：`cd kb_qa_system/frontend && npx.cmd vitest run` — 全部通过
4. 前端构建：`cd kb_qa_system/frontend && npm.cmd run build` — 成功
5. 迁移可逆：`alembic upgrade head` → `alembic downgrade -1` → `alembic upgrade head`
6. 新增功能手动验证要点：
   - 文档分支 CRUD 端点可调用
   - 审计日志表存在且记录正确
   - 全文检索返回结果
   - IME 输入不误触发搜索
   - 暗色模式切换正常
   - 帮助页面可访问
